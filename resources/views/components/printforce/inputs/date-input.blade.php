<div class="mb-3">
    <label for="" class="form-label">{{ $label }}</label>
    <input
        type="date"
        class="form-control datepicker-input"
        name="{{ $name }}"
        id="{{ $id }}"
        value="{{ $value }}"
    />
</div>
